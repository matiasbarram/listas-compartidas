
// newItem={newItem} setNewItem={setNewItem} createProduct={createProduct} closeModal={closeModal}
export default function ({ newItem, setNewItem, createProduct, closeModal }: any) {
    return (
        <div>test item form</div>
    )
}